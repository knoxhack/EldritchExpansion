// Build configuration for the Obsidian Forgemaster module
apply from: '../../common.gradle'

base {
    archivesName = "obsidian-forgemaster"
}

neoForge {
    mods {
        "eldritchvoid_obsidianforgemaster" {
            sourceSet(sourceSets.main)
            displayName = "Eldritch Void - Obsidian Forgemaster"
        }
    }
}

// Include resources generated by data generators.
sourceSets.main.resources {
    srcDir 'src/generated/resources'
}

dependencies {
    // Add module-specific dependencies here
    
    // Module depends on the core
    implementation project(':core')
}

// Copy sources from the main project that belong to this module
task copyModuleSources(type: Copy) {
    from '../../src/main/java/com/eldritchvoid/modules/obsidianforgemaster'
    into 'src/main/java/com/eldritchvoid/modules/obsidianforgemaster'
}

// Copy module-specific resources
task copyModuleResources(type: Copy) {
    from '../../src/main/resources/assets/eldritchvoid/modules/obsidianforgemaster'
    into 'src/main/resources/assets/eldritchvoid/modules/obsidianforgemaster'
    
    from '../../src/main/resources/data/eldritchvoid/modules/obsidianforgemaster'
    into 'src/main/resources/data/eldritchvoid/modules/obsidianforgemaster'
}

// Make compileJava depend on the copy tasks
compileJava.dependsOn copyModuleSources
processResources.dependsOn copyModuleResources

// Ensure directories exist
task createDirectories {
    doLast {
        mkdir "src/main/java/com/eldritchvoid/modules/obsidianforgemaster"
        mkdir "src/main/resources/assets/eldritchvoid/modules/obsidianforgemaster"
        mkdir "src/main/resources/data/eldritchvoid/modules/obsidianforgemaster"
        mkdir "src/generated/resources"
    }
}

copyModuleSources.dependsOn createDirectories
copyModuleResources.dependsOn createDirectories