// Build configuration for the Eldritch Artifacts module
apply from: '../../common.gradle'

base {
    archivesName = "eldritch-artifacts"
}

neoForge {
    mods {
        "eldritchvoid_eldritchartifacts" {
            sourceSet(sourceSets.main)
            displayName = "Eldritch Void - Eldritch Artifacts"
        }
    }
}

// Include resources generated by data generators.
sourceSets.main.resources {
    srcDir 'src/generated/resources'
}

dependencies {
    // Add module-specific dependencies here
    
    // Module depends on the core
    implementation project(':core')
}

// Copy sources from the main project that belong to this module
task copyModuleSources(type: Copy) {
    from '../../src/main/java/com/eldritchvoid/modules/eldritchartifacts'
    into 'src/main/java/com/eldritchvoid/modules/eldritchartifacts'
}

// Copy module-specific resources
task copyModuleResources(type: Copy) {
    from '../../src/main/resources/assets/eldritchvoid/modules/eldritchartifacts'
    into 'src/main/resources/assets/eldritchvoid/modules/eldritchartifacts'
    
    from '../../src/main/resources/data/eldritchvoid/modules/eldritchartifacts'
    into 'src/main/resources/data/eldritchvoid/modules/eldritchartifacts'
}

// Make compileJava depend on the copy tasks
compileJava.dependsOn copyModuleSources
processResources.dependsOn copyModuleResources

// Ensure directories exist
task createDirectories {
    doLast {
        mkdir "src/main/java/com/eldritchvoid/modules/eldritchartifacts"
        mkdir "src/main/resources/assets/eldritchvoid/modules/eldritchartifacts"
        mkdir "src/main/resources/data/eldritchvoid/modules/eldritchartifacts"
        mkdir "src/generated/resources"
    }
}

copyModuleSources.dependsOn createDirectories
copyModuleResources.dependsOn createDirectories